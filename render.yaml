services:
  - type: web
    name: step8up-week8-node-express-blog
    runtime: node
    repo: https://github.com/KamilWo/step8up-week8-node-express-blog
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        sync: false
      - key: NODE_ENV
        sync: false
    region: virginia
    buildCommand: yarn --frozen-lockfile install
    startCommand: node src/server/server.js
    healthCheckPath: /healthz
    autoDeployTrigger: "off"
version: "1"
